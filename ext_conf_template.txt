# cat=basic/enable/30; type=integer; label=Days after voting from same IP address is allowed. Set to 0 to ignore IP restriction.
daysForNextCount = 0
# cat=basic/enable/31; type=boolean; label=Count visits as well, if the user is logged into the backend, otherwise users with a backend login will not be counted.
countVisitsAlsoIfUserIsLoggedIntoBackend = 0
